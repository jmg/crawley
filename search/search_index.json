{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"crawley","text":"<p>A pythonic crawling / scraping framework for Python 3, built on <code>asyncio</code> + <code>httpx</code>.</p> <p>crawley lets you crawl websites and extract structured data with a tiny, declarative API. This is the modernized release: the legacy <code>eventlet</code> / <code>elixir</code> stack has been replaced by asyncio, httpx and SQLAlchemy 2.x.</p>"},{"location":"#two-ways-to-use-it","title":"Two ways to use it","text":"<p>=== \"As a scraping library\"</p> <pre><code>The fastest way to pull data out of a page:\n\n```python\nfrom crawley.scraping import fetch\n\ndoc = fetch(\"https://quotes.toscrape.com/\")\nfor quote in doc.css(\"div.quote\"):\n    print(quote.css_first(\"small.author\").text,\n          \"-&gt;\",\n          quote.css_first(\"span.text\").text)\n```\n\nSee [Scraping API](scraping.md).\n</code></pre> <p>=== \"As a crawling framework\"</p> <pre><code>Define crawlers and scrapers declaratively and let crawley walk the site:\n\n```python\nfrom crawley.crawlers import BaseCrawler\nfrom crawley.scrapers import BaseScraper\n\nclass QuotesScraper(BaseScraper):\n    matching_urls = [\"%quotes.toscrape.com%\"]\n\n    def scrape(self, response):\n        for q in response.css(\"div.quote\"):\n            print(q.css_first(\"span.text\").text)\n\nclass QuotesCrawler(BaseCrawler):\n    start_urls = [\"https://quotes.toscrape.com/\"]\n    scrapers = [QuotesScraper]\n    max_depth = 2\n\nQuotesCrawler().run()\n```\n\nSee [Crawlers &amp; Scrapers](crawler.md).\n</code></pre>"},{"location":"#features","title":"Features","text":"<ul> <li>High speed asynchronous crawler powered by <code>asyncio</code> + <code>httpx</code>.</li> <li>Extract data with XPath, CSS selectors or PyQuery.</li> <li>A modern, ergonomic scraping API (<code>fetch</code>, <code>Document</code>, <code>extract</code>).</li> <li>Politeness: <code>robots.txt</code>, per-host rate limiting and   retries with exponential backoff.</li> <li>Persistence: SQL (SQLAlchemy 2.x), MongoDB, CouchDB and   JSON / XML / CSV exports.</li> <li>A small DSL and CLI (<code>crawley startproject</code>, <code>crawley run</code>, ...).</li> </ul>"},{"location":"#requirements","title":"Requirements","text":"<ul> <li>Python 3.9+</li> </ul>"},{"location":"#runnable-examples","title":"Runnable examples","text":"<p>The <code>examples/</code> folder has small, self-contained scripts you can run directly:</p> <ul> <li><code>01_scraping_quickstart.py</code> \u2014 the scraping API.</li> <li><code>02_crawler.py</code> \u2014 a crawler that follows pagination.</li> <li><code>03_polite_crawler.py</code> \u2014 robots.txt, rate limiting and retries.</li> <li><code>04_persistence_json.py</code> \u2014 persisting to a JSON document.</li> <li><code>05_concurrent_fetch.py</code> \u2014 concurrent fetching with <code>afetch_all</code>.</li> </ul> <p>Continue with the installation guide.</p>"},{"location":"cli/","title":"CLI &amp; Framework","text":"<p>Installing crawley provides a <code>crawley</code> command to scaffold and run projects.</p>"},{"location":"cli/#commands","title":"Commands","text":"Command Description <code>crawley startproject NAME</code> Scaffold a new project. <code>crawley run</code> Sync the database and run the project's crawlers. <code>crawley syncdb</code> Create the database from <code>models.py</code>. <code>crawley migratedb</code> Drop and recreate the tables. <code>crawley shell URL</code> Fetch a url and open a Python shell to scrape it. <code>crawley browser URL</code> Open the visual scraping browser (needs <code>[gui]</code>). <p><code>startproject</code> accepts a <code>-t/--type</code>: <code>code</code> (default), <code>template</code> (DSL) or <code>database</code>.</p>"},{"location":"cli/#project-layout","title":"Project layout","text":"<pre><code>crawley startproject myproject\ncd myproject\n</code></pre> <pre><code>myproject/\n\u251c\u2500\u2500 settings.py            # project configuration\n\u2514\u2500\u2500 myproject/\n    \u251c\u2500\u2500 models.py          # your entities\n    \u2514\u2500\u2500 crawlers.py        # your crawlers + scrapers\n</code></pre>"},{"location":"cli/#modelspy","title":"models.py","text":"<pre><code>from crawley.persistance import Entity, UrlEntity, Field, Unicode\n\nclass Package(Entity):\n    package = Field(Unicode(255))\n    description = Field(Unicode(255))\n</code></pre>"},{"location":"cli/#crawlerspy","title":"crawlers.py","text":"<pre><code>from crawley.crawlers import BaseCrawler\nfrom crawley.scrapers import BaseScraper\nfrom crawley.extractors import XPathExtractor\nfrom models import *\n\nclass PackageScraper(BaseScraper):\n    matching_urls = [\"%\"]\n\n    def scrape(self, response):\n        for row in response.css(\"div.package\"):\n            Package(package=row.css_first(\"h2::text\"))\n\nclass PackageCrawler(BaseCrawler):\n    start_urls = [\"https://pypi.org/\"]\n    scrapers = [PackageScraper]\n    max_depth = 1\n    extractor = XPathExtractor\n</code></pre>"},{"location":"cli/#settingspy","title":"settings.py","text":"<p>Relevant keys:</p> <pre><code>DATABASE_ENGINE = \"sqlite\"       # sqlite, postgres, mysql, oracle\nDATABASE_NAME = \"myproject\"\nJSON_DOCUMENT = \"\"               # set a filename to dump JSON\nXML_DOCUMENT = \"\"\nCSV_DOCUMENT = \"\"\n# MONGO_DB_HOST / MONGO_DB_NAME, COUCH_DB_HOST / COUCH_DB_NAME\nMAX_CONCURRENCY = 100\nSHOW_DEBUG_INFO = True\n</code></pre> <p>Then run:</p> <pre><code>crawley run\n</code></pre>"},{"location":"cli/#the-dsl-template-projects","title":"The DSL (template projects)","text":"<p>For simple cases you can describe a scraper declaratively instead of writing Python. Create a <code>template</code> project and write a <code>.crw</code> template:</p> <pre><code>PAGE =&gt; http://example.com/\npackages.name -&gt; /html/body//h2\npackages.author -&gt; /html/body//span[@class=\"author\"]\n</code></pre> <ul> <li><code>PAGE =&gt; url</code> declares the template page (its structure is matched against   candidate pages via <code>SmartScraper</code>).</li> <li><code>table.column -&gt; xpath</code> maps an XPath result to an entity column.</li> </ul> <p>crawley compiles this into entities and scrapers at runtime. The companion <code>config.ini</code> provides <code>start_urls</code> and <code>max_depth</code>:</p> <pre><code>[crawler]\nstart_urls = http://example.com/\nmax_depth = 1\n</code></pre>"},{"location":"crawler/","title":"Crawlers &amp; Scrapers","text":"<p>A crawler walks a site starting from <code>start_urls</code>, and delegates data extraction to one or more scrapers.</p>"},{"location":"crawler/#a-minimal-crawler","title":"A minimal crawler","text":"<pre><code>from crawley.crawlers import BaseCrawler\nfrom crawley.scrapers import BaseScraper\nfrom crawley.extractors import XPathExtractor\n\n\nclass QuotesScraper(BaseScraper):\n    # only pages matching these patterns are scraped (\"%\" is a wildcard)\n    matching_urls = [\"%quotes.toscrape.com%\"]\n\n    def scrape(self, response):\n        for quote in response.css(\"div.quote\"):\n            print(quote.css_first(\"span.text\").text)\n\n\nclass QuotesCrawler(BaseCrawler):\n    start_urls = [\"https://quotes.toscrape.com/\"]\n    scrapers = [QuotesScraper]\n    max_depth = 2\n    extractor = XPathExtractor      # or CSSExtractor / PyQueryExtractor\n\n\nQuotesCrawler().run()               # synchronous entry point\n# await QuotesCrawler().start()     # ...or from your own event loop\n</code></pre> <p><code>run()</code> is a convenience wrapper around <code>asyncio.run(self.start())</code>.</p>"},{"location":"crawler/#crawler-options","title":"Crawler options","text":"Attribute Default Description <code>start_urls</code> <code>[]</code> Where the crawl begins. <code>scrapers</code> <code>[]</code> Scraper classes to run on each page. <code>max_depth</code> <code>-1</code> Recursion depth (<code>-1</code> = unlimited). <code>allowed_urls</code> <code>[]</code> Only crawl urls matching these patterns. <code>black_list</code> <code>[]</code> Never crawl urls matching these patterns. <code>extractor</code> <code>XPathExtractor</code> How <code>response.html</code> is parsed. <code>max_concurrency_level</code> <code>100</code> Max in-flight requests. <code>requests_delay</code> <code>0.5</code> Average delay between requests (s). <code>requests_deviation</code> <code>0.25</code> Random jitter on the delay (s). <code>search_all_urls</code> <code>True</code> Follow links found on the page. <code>search_hidden_urls</code> <code>False</code> Also discover urls via a regex over the html. <code>unique_urls</code> <code>True</code> Skip already-visited urls (prevents loops). <code>post_urls</code> <code>[]</code> <code>(pattern, data)</code> tuples to issue POSTs. <code>login</code> <code>None</code> <code>(url, data)</code> to authenticate before crawling. <code>unique_urls</code> <code>True</code> Skip already-visited urls. <code>http_cache</code> <code>False</code> Cache responses on disk (development helper). <code>http_cache_dir</code> <code>.crawley_cache</code> Where the HTTP cache is stored. <code>render_js</code> <code>False</code> Render pages with Playwright (<code>crawley[js]</code>). <p>See Politeness for <code>respect_robots</code>, <code>crawl_delay</code>, <code>max_concurrency_per_host</code> and the retry options.</p>"},{"location":"crawler/#stats","title":"Stats","text":"<p>Every crawler / spider owns a <code>stats</code> collector. It counts requests, responses, per-status codes, errors, robots blocks, items (spiders) and the elapsed time, and logs a summary when the crawl finishes:</p> <pre><code>crawler = MyCrawler()\ncrawler.run()\nprint(crawler.stats.get_stats())\n# {'requests': 12, 'responses': 12, 'status/200': 12, 'elapsed_seconds': 1.3, ...}\n</code></pre>"},{"location":"crawler/#http-cache-development","title":"HTTP cache (development)","text":"<p>Set <code>http_cache = True</code> to cache every response on disk (keyed by method + url + body). Re-running the crawl then serves from the cache instead of hitting the site again \u2014 handy while developing scrapers:</p> <pre><code>class MyCrawler(BaseCrawler):\n    start_urls = [\"https://example.com/\"]\n    http_cache = True\n    http_cache_dir = \".crawley_cache\"\n</code></pre>"},{"location":"crawler/#url-matching","title":"URL matching","text":"<p>Patterns use <code>%</code> as a wildcard:</p> Pattern Matches <code>\"%foo%\"</code> <code>foo</code> anywhere in the url <code>\"foo%\"</code> url starts with <code>foo</code> <code>\"%foo\"</code> url ends with <code>foo</code> <code>\"foo\"</code> exact match"},{"location":"crawler/#extractors","title":"Extractors","text":"<p><code>response.html</code> is produced by the crawler's <code>extractor</code>:</p> Extractor <code>response.html</code> is... Query with <code>XPathExtractor</code> an <code>lxml</code> tree <code>.xpath(...)</code> <code>CSSExtractor</code> an <code>lxml</code> tree <code>.getroot().cssselect(...)</code> <code>PyQueryExtractor</code> a <code>PyQuery</code> object <code>pq(\"div.foo\")</code> <code>RawExtractor</code> the raw html <code>str</code> anything <p>Regardless of the extractor, the modern shortcuts (<code>response.css</code>, <code>response.css_first</code>, <code>response.extract</code>, <code>response.doc</code>) are always available.</p>"},{"location":"crawler/#events","title":"Events","text":"<p>Override these hooks to react to the crawl lifecycle:</p> <pre><code>class MyCrawler(BaseCrawler):\n    def on_start(self): ...\n    def on_finish(self): ...\n    def on_request_error(self, url, ex): ...\n    def on_robots_blocked(self, url): ...\n</code></pre>"},{"location":"crawler/#post-requests-and-login","title":"POST requests and login","text":"<pre><code>class MyCrawler(BaseCrawler):\n    start_urls = [\"https://site/search\"]\n    post_urls = [(\"%/search%\", {\"q\": \"crawley\"})]\n    login = (\"https://site/login\", {\"user\": \"me\", \"password\": \"secret\"})\n</code></pre>"},{"location":"crawler/#specialized-crawlers","title":"Specialized crawlers","text":"<ul> <li><code>FastCrawler</code> \u2014 issues requests without the per-request delay.</li> <li><code>OffLineCrawler</code> \u2014 rewrites relative <code>link</code>/<code>img</code> urls to absolute ones   so saved pages render offline.</li> </ul>"},{"location":"crawler/#smartscraper","title":"SmartScraper","text":"<p><code>SmartScraper</code> only scrapes pages whose html structure is similar to a template page, which is handy when a site has many pages sharing a layout:</p> <pre><code>from crawley.scrapers import SmartScraper\n\nclass ProductScraper(SmartScraper):\n    matching_urls = [\"%/product/%\"]\n    template_url = \"https://shop.example/product/1\"\n    ratio = 0.45     # similarity threshold\n\n    def scrape(self, response):\n        ...\n</code></pre>"},{"location":"installation/","title":"Installation","text":"<p>crawley requires Python 3.9+.</p>"},{"location":"installation/#from-pypi","title":"From PyPI","text":"<pre><code>pip install crawley\n</code></pre> <p>The core install pulls in <code>httpx</code>, <code>lxml</code>, <code>pyquery</code> and <code>cssselect</code> \u2014 enough for crawling and the scraping API.</p>"},{"location":"installation/#optional-extras","title":"Optional extras","text":"<p>Some features need extra dependencies. Install only what you use:</p> Extra Installs Enables <code>sql</code> SQLAlchemy 2.x Relational persistence (SQLite/PG/MySQL) <code>mongo</code> pymongo MongoDB storage <code>shell</code> IPython A nicer <code>crawley shell</code> <code>gui</code> PySide6 The visual scraping browser <code>http2</code> h2 HTTP/2 support in httpx <code>dev</code> pytest, ruff, ... Running the test suite <code>docs</code> mkdocs-material Building this documentation <pre><code>pip install \"crawley[sql]\"\npip install \"crawley[sql,mongo]\"\n</code></pre>"},{"location":"installation/#from-source","title":"From source","text":"<pre><code>git clone https://github.com/jmg/crawley\ncd crawley\npip install -e \".[dev]\"\npytest -q\n</code></pre>"},{"location":"installation/#building-the-docs","title":"Building the docs","text":"<pre><code>pip install -e \".[docs]\"\nmkdocs serve     # live preview on http://127.0.0.1:8000\nmkdocs build     # static site in ./site\n</code></pre>"},{"location":"persistence/","title":"Persistence","text":"<p>crawley can persist scraped data to relational databases, NoSQL stores or plain documents. Sessions are passed to the crawler and committed as the crawl progresses.</p>"},{"location":"persistence/#relational-sqlalchemy-2x","title":"Relational (SQLAlchemy 2.x)","text":"<p>Requires the <code>sql</code> extra (<code>pip install \"crawley[sql]\"</code>).</p> <p>Define entities by subclassing <code>Entity</code>. Instantiating an entity stages it in the shared session:</p> <pre><code>from crawley.persistance import Entity, UrlEntity, Field, Unicode\n\nclass Package(Entity):\n    package = Field(Unicode(255))\n    description = Field(Unicode(255))\n\nclass Urls(UrlEntity):     # has href / parent columns\n    pass\n</code></pre> <p><code>Field</code> and <code>Unicode</code> are thin shims over SQLAlchemy's <code>Column</code> and column types. Set up the engine and create the tables with <code>setup()</code>:</p> <pre><code>from crawley.persistance import session, setup\n\nsetup(\"sqlite:///packages.sqlite\")\n\nPackage(package=\"crawley\", description=\"modern crawler\")\nsession.commit()\n</code></pre> <p>Supported engines (via <code>crawley.persistance.relational.connectors</code>): SQLite, PostgreSQL, MySQL, Oracle.</p>"},{"location":"persistence/#documents-json-xml-csv","title":"Documents \u2014 JSON / XML / CSV","text":"<p>No extra dependencies. Subclass the document type; each instance becomes a row, and the matching session writes the file on <code>commit()</code>:</p> <pre><code>from crawley.persistance.documents import JSONDocument, json_session\n\nclass Quote(JSONDocument):\n    pass\n\nQuote(text=\"...\", author=\"...\")\njson_session.file_name = \"quotes.json\"\njson_session.commit()\n</code></pre> <p><code>XMLDocument</code> / <code>xml_session</code> and <code>CSVDocument</code> / <code>csv_session</code> work the same way.</p>"},{"location":"persistence/#nosql-mongodb-couchdb","title":"NoSQL \u2014 MongoDB / CouchDB","text":"<p>MongoDB requires the <code>mongo</code> extra; CouchDB talks to the HTTP API directly via httpx.</p> <pre><code>from crawley.persistance.nosql import MongoEntity, mongo_session\n\nclass Package(MongoEntity):\n    pass\n\nPackage(name=\"crawley\", stars=42)\n# configured + committed by the crawler via settings (see CLI docs)\n</code></pre>"},{"location":"persistence/#using-sessions-in-a-crawler","title":"Using sessions in a crawler","text":"<p>Pass the sessions you want committed to the crawler. After each successful scrape, crawley calls <code>commit()</code> on every session:</p> <pre><code>import asyncio\nfrom crawley.crawlers import BaseCrawler\nfrom crawley.scrapers import BaseScraper\nfrom crawley.persistance.documents import JSONDocument, json_session\n\njson_session.file_name = \"out.json\"\n\nclass Quote(JSONDocument):\n    pass\n\nclass Scraper(BaseScraper):\n    matching_urls = [\"%\"]\n    def scrape(self, response):\n        Quote(title=response.css_first(\"h1\").text)\n\nclass Crawler(BaseCrawler):\n    start_urls = [\"https://quotes.toscrape.com/\"]\n    scrapers = [Scraper]\n\nasyncio.run(Crawler(sessions=[json_session]).start())\n</code></pre> <p>When using the CLI, the storages are wired up automatically from your <code>settings.py</code> (<code>DATABASE_*</code>, <code>JSON_DOCUMENT</code>, <code>MONGO_DB_HOST</code>, ...).</p>"},{"location":"politeness/","title":"Politeness \u2014 robots, rate limiting, retries","text":"<p>crawley ships the building blocks to crawl responsibly and robustly: it can honour <code>robots.txt</code>, throttle requests per host and retry transient failures with exponential backoff.</p>"},{"location":"politeness/#robotstxt","title":"robots.txt","text":"<p>Set <code>respect_robots = True</code> to make the crawler fetch and obey each host's <code>robots.txt</code>:</p> <pre><code>class MyCrawler(BaseCrawler):\n    start_urls = [\"https://example.com/\"]\n    respect_robots = True\n</code></pre> <ul> <li>Each host's <code>robots.txt</code> is fetched once and cached.</li> <li>Disallowed urls are skipped and <code>on_robots_blocked(url)</code> is called.</li> <li>A <code>404</code> robots.txt allows everything; a <code>401</code>/<code>403</code> disallows everything.</li> <li>A <code>Crawl-delay</code> directive is automatically applied as the per-host delay.</li> </ul> <pre><code>class MyCrawler(BaseCrawler):\n    respect_robots = True\n\n    def on_robots_blocked(self, url):\n        print(\"skipped (robots):\", url)\n</code></pre> <p>You can use the policy on its own too:</p> <pre><code>import httpx\nfrom crawley.http.robots import RobotsPolicy\n\npolicy = RobotsPolicy(user_agent=\"my-bot\")\nasync with httpx.AsyncClient() as client:\n    await policy.allowed(\"https://example.com/private\", client)  # -&gt; bool\n</code></pre>"},{"location":"politeness/#per-host-rate-limiting","title":"Per-host rate limiting","text":"<p>Two knobs control how hard a single host is hit:</p> <pre><code>class MyCrawler(BaseCrawler):\n    crawl_delay = 1.0                 # &gt;= 1s between requests to the same host\n    max_concurrency_per_host = 2      # at most 2 concurrent requests per host\n</code></pre> <p><code>crawl_delay</code> enforces a minimum spacing between consecutive requests to the same host, while <code>max_concurrency_level</code> still bounds the global number of in-flight requests. Different hosts are throttled independently.</p> <p>The underlying primitive is <code>crawley.http.throttle.HostRateLimiter</code>.</p>"},{"location":"politeness/#retries-with-backoff","title":"Retries with backoff","text":"<p>Failed requests \u2014 network errors and retryable HTTP statuses such as <code>429</code>, <code>500</code>, <code>502</code>, <code>503</code>, <code>504</code> \u2014 are retried automatically:</p> <pre><code>class MyCrawler(BaseCrawler):\n    max_retries = 3                   # attempts after the first failure\n    retry_backoff = 0.5               # base seconds for the backoff\n    retry_statuses = {429, 503}       # which statuses to retry\n</code></pre> <ul> <li>Backoff is exponential with jitter: roughly   <code>retry_backoff * 2 ** attempt</code>, capped at 30s.</li> <li>A <code>Retry-After</code> response header (seconds or HTTP date) takes precedence.</li> <li>After exhausting the retries, the last response is returned (so you can   inspect a final <code>503</code>); persistent network errors are re-raised and routed   to <code>on_request_error</code>.</li> </ul> <p>The policy is <code>crawley.http.retry.RetryPolicy</code> and can be used standalone:</p> <pre><code>from crawley.http.retry import RetryPolicy\n\npolicy = RetryPolicy(max_retries=5, backoff_factor=0.5, statuses={503})\npolicy.should_retry(attempt=0, response=resp)   # -&gt; bool\npolicy.backoff_time(attempt=2)                  # -&gt; seconds\n</code></pre>"},{"location":"politeness/#autothrottle","title":"AutoThrottle","text":"<p>Instead of a fixed <code>crawl_delay</code>, let crawley adapt the per-host delay to the observed response latency, aiming to keep roughly <code>target_concurrency</code> requests in flight per host:</p> <pre><code>class MyCrawler(BaseCrawler):\n    start_urls = [\"https://example.com/\"]\n    autothrottle = True\n    autothrottle_target_concurrency = 2.0   # ~2 concurrent req/host\n    autothrottle_start_delay = 1.0\n    autothrottle_max_delay = 30.0\n</code></pre> <p>After each response the per-host delay is nudged towards <code>latency / target_concurrency</code> (smoothed and clamped to <code>max_delay</code>). Slower servers are hit more gently, faster ones a bit harder. The primitive is <code>crawley.http.autothrottle.AutoThrottle</code>.</p>"},{"location":"politeness/#putting-it-together","title":"Putting it together","text":"<pre><code>class PoliteCrawler(BaseCrawler):\n    start_urls = [\"https://example.com/\"]\n    respect_robots = True\n    crawl_delay = 1.0\n    max_concurrency_per_host = 2\n    max_retries = 3\n    retry_backoff = 0.5\n</code></pre>"},{"location":"reference/","title":"API Reference","text":"<p>Auto-generated from the docstrings.</p>"},{"location":"reference/#scraping","title":"Scraping","text":""},{"location":"reference/#crawley.scraping.fetch","title":"<code>crawley.scraping.fetch(url, **kwargs)</code>","text":"<p>Fetch url and return a parsed :class:<code>Document</code> (synchronous).</p>"},{"location":"reference/#crawley.scraping.afetch","title":"<code>crawley.scraping.afetch(url, client=None, **kwargs)</code>  <code>async</code>","text":"<p>Fetch url asynchronously and return a :class:<code>Document</code>.</p>"},{"location":"reference/#crawley.scraping.afetch_all","title":"<code>crawley.scraping.afetch_all(urls, **kwargs)</code>  <code>async</code>","text":"<p>Fetch many urls concurrently, returning a list of :class:<code>Document</code>.</p>"},{"location":"reference/#crawley.scraping.scrape","title":"<code>crawley.scraping.scrape(url, rules, **kwargs)</code>","text":"<p>Fetch url and immediately :meth:<code>Document.extract</code> it with rules.</p>"},{"location":"reference/#crawley.scraping.parse","title":"<code>crawley.scraping.parse(html, url=None)</code>","text":"<p>Parse an html string into a :class:<code>Document</code>.</p>"},{"location":"reference/#crawley.scraping.Document","title":"<code>crawley.scraping.Document</code>","text":"<p>               Bases: <code>Element</code></p> <p>A parsed html document ready to be scraped.</p>"},{"location":"reference/#crawley.scraping.Document.title","title":"<code>title</code>  <code>property</code>","text":"<p>The page <code>&lt;title&gt;</code> text, if any.</p>"},{"location":"reference/#crawley.scraping.Document.extract","title":"<code>extract(rules)</code>","text":"<p>Extract a dict of fields from the document.</p> <p>rules maps field names to selectors. A plain string selector yields a single value (the first match); a one-element list selector yields the list of every match::</p> <pre><code>doc.extract({\n    \"title\": \"h1::text\",\n    \"price\": \"span.price::text\",\n    \"images\": [\"img::attr(src)\"],\n})\n</code></pre>"},{"location":"reference/#crawley.scraping.Document.links","title":"<code>links(selector='a')</code>","text":"<p>Return the (absolute, de-duplicated) hrefs found in the page.</p>"},{"location":"reference/#crawley.scraping.Element","title":"<code>crawley.scraping.Element</code>","text":"<p>A thin, convenient wrapper around an <code>lxml</code> html element.</p>"},{"location":"reference/#crawley.scraping.Element.attrs","title":"<code>attrs</code>  <code>property</code>","text":"<p>A dict of the element's attributes.</p>"},{"location":"reference/#crawley.scraping.Element.html","title":"<code>html</code>  <code>property</code>","text":"<p>The element serialized back to html.</p>"},{"location":"reference/#crawley.scraping.Element.text","title":"<code>text</code>  <code>property</code>","text":"<p>The normalized recursive text of the element.</p>"},{"location":"reference/#crawley.scraping.Element.attr","title":"<code>attr(name, default=None)</code>","text":"<p>Return the name attribute (or default).</p>"},{"location":"reference/#crawley.scraping.Element.css","title":"<code>css(selector)</code>","text":"<p>Return the descendants matching the CSS selector.</p>"},{"location":"reference/#crawley.scraping.Element.css_first","title":"<code>css_first(selector)</code>","text":"<p>Return the first descendant matching selector (or <code>None</code>).</p>"},{"location":"reference/#crawley.scraping.Element.xpath","title":"<code>xpath(query)</code>","text":"<p>Return the result of an XPath query.</p> <p>String results (e.g. <code>\"//a/@href\"</code>) are returned as-is; element results are wrapped in :class:<code>Element</code>.</p>"},{"location":"reference/#crawlers","title":"Crawlers","text":""},{"location":"reference/#crawley.crawlers.base.BaseCrawler","title":"<code>crawley.crawlers.base.BaseCrawler</code>","text":"<p>User crawlers must inherit from this class.</p> <p>Override the relevant methods and define <code>start_urls</code>, <code>scrapers</code> and the <code>max_depth</code> to control the crawl.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.allowed_urls","title":"<code>allowed_urls = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>A list of url patterns allowed for crawl.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.autothrottle","title":"<code>autothrottle = False</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Adapt the per-host delay to the observed response latency.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.autothrottle_max_delay","title":"<code>autothrottle_max_delay = 60.0</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Maximum per-host delay AutoThrottle may set (seconds).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.autothrottle_start_delay","title":"<code>autothrottle_start_delay = 1.0</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Initial per-host delay used by AutoThrottle (seconds).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.autothrottle_target_concurrency","title":"<code>autothrottle_target_concurrency = 1.0</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Target number of concurrent requests per host for AutoThrottle.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.black_list","title":"<code>black_list = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>A list of blocked url patterns that are never crawled.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.crawl_delay","title":"<code>crawl_delay = config.CRAWL_DELAY</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Minimum seconds between two requests to the same host.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.extractor","title":"<code>extractor = extractor_class()</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The extractor class. Defaults to :class:<code>XPathExtractor</code>.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.headers","title":"<code>headers = {}</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The default request headers.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.http_cache","title":"<code>http_cache = False</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Cache responses on disk (development helper).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.http_cache_dir","title":"<code>http_cache_dir = '.crawley_cache'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Directory used by the on-disk HTTP cache.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.login","title":"<code>login = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Login data: a tuple of <code>(url, login_dict)</code>.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.max_concurrency_level","title":"<code>max_concurrency_level = None</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The maximum number of concurrent requests.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.max_concurrency_per_host","title":"<code>max_concurrency_per_host = config.MAX_CONCURRENCY_PER_HOST</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Maximum simultaneous requests per host (<code>None</code> disables the limit).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.max_depth","title":"<code>max_depth = -1</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The maximum crawling recursive level (<code>-1</code> means unlimited).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.max_retries","title":"<code>max_retries = config.REQUEST_MAX_RETRIES</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>How many times a failed request is retried.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.playwright_options","title":"<code>playwright_options = {}</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Extra options for the Playwright manager (browser_type, headless, ...).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.post_urls","title":"<code>post_urls = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>POST data for urls: a list of <code>(url, data_dict)</code> tuples.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.render_js","title":"<code>render_js = False</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Render pages with a headless browser (Playwright). Needs <code>crawley[js]</code>.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.requests_delay","title":"<code>requests_delay = config.REQUEST_DELAY</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The average delay time between requests.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.requests_deviation","title":"<code>requests_deviation = config.REQUEST_DEVIATION</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>The requests deviation time.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.respect_robots","title":"<code>respect_robots = config.RESPECT_ROBOTS</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>When <code>True</code> the crawler honours each site's <code>robots.txt</code>.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.retry_backoff","title":"<code>retry_backoff = config.RETRY_BACKOFF_FACTOR</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Base seconds for the exponential retry backoff.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.retry_statuses","title":"<code>retry_statuses = config.RETRY_STATUSES</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>HTTP status codes that trigger a retry.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.scrapers","title":"<code>scrapers = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>A list of scraper classes.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.search_all_urls","title":"<code>search_all_urls = True</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Search for urls in the page when scrapers don't return any.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.search_hidden_urls","title":"<code>search_hidden_urls = False</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Search for urls hidden anywhere in the html (not only <code>&lt;a&gt;</code> tags).</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.start_urls","title":"<code>start_urls = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>A list containing the start urls for the crawler.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.unique_urls","title":"<code>unique_urls = True</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Skip urls that have already been visited during the crawl.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.get_urls","title":"<code>get_urls(response)</code>","text":"<p>Return the urls found in the current html page.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.on_finish","title":"<code>on_finish()</code>","text":"<p>Override to run code when the crawler finishes.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.on_request_error","title":"<code>on_request_error(url, ex)</code>","text":"<p>Override to customize the request error handler.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.on_robots_blocked","title":"<code>on_robots_blocked(url)</code>","text":"<p>Override to react when robots.txt disallows crawling url.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.on_start","title":"<code>on_start()</code>","text":"<p>Override to run code when the crawler starts.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.run","title":"<code>run()</code>","text":"<p>Convenience synchronous entry point.</p>"},{"location":"reference/#crawley.crawlers.base.BaseCrawler.start","title":"<code>start()</code>  <code>async</code>","text":"<p>Run the crawler (coroutine).</p>"},{"location":"reference/#crawley.crawlers.fast.FastCrawler","title":"<code>crawley.crawlers.fast.FastCrawler</code>","text":"<p>               Bases: <code>BaseCrawler</code></p> <p>Like :class:<code>BaseCrawler</code> but issues requests without delays.</p>"},{"location":"reference/#crawley.crawlers.offline.OffLineCrawler","title":"<code>crawley.crawlers.offline.OffLineCrawler</code>","text":"<p>               Bases: <code>BaseCrawler</code></p> <p>A crawler that fixes relative asset urls in the fetched html.</p>"},{"location":"reference/#scrapers","title":"Scrapers","text":""},{"location":"reference/#crawley.scrapers.base.BaseScraper","title":"<code>crawley.scrapers.base.BaseScraper</code>","text":"<p>User scrapers must inherit from this class.</p> <p>Implement :meth:<code>scrape</code> with the data extraction logic and define the <code>matching_urls</code> that this scraper is able to process.</p>"},{"location":"reference/#crawley.scrapers.base.BaseScraper.get_urls","title":"<code>get_urls(response)</code>","text":"<p>Return a list of urls found in the current html.</p>"},{"location":"reference/#crawley.scrapers.base.BaseScraper.on_cannot_scrape","title":"<code>on_cannot_scrape(response)</code>","text":"<p>Customize the can't-scrape handler.</p>"},{"location":"reference/#crawley.scrapers.base.BaseScraper.on_scrape_error","title":"<code>on_scrape_error(response, ex)</code>","text":"<p>Customize the scrape error handler.</p>"},{"location":"reference/#crawley.scrapers.base.BaseScraper.scrape","title":"<code>scrape(response)</code>","text":"<p>Define the data you want to extract here.</p>"},{"location":"reference/#crawley.scrapers.base.BaseScraper.try_scrape","title":"<code>try_scrape(response)</code>","text":"<p>Try to parse the html page, returning the urls it discovers.</p>"},{"location":"reference/#crawley.scrapers.smart.SmartScraper","title":"<code>crawley.scrapers.smart.SmartScraper</code>","text":"<p>               Bases: <code>BaseScraper</code></p> <p>Scrape only pages whose html structure is similar to a template page.</p> <p>The structure of <code>template_url</code> is fetched once (synchronously) at construction time and every candidate page is compared against it.</p>"},{"location":"reference/#spiders","title":"Spiders","text":""},{"location":"reference/#crawley.spider.Spider","title":"<code>crawley.spider.Spider</code>","text":"<p>               Bases: <code>BaseCrawler</code></p> <p>A callback-driven spider.</p> <p>Define :meth:<code>parse</code> (the default callback) and yield :class:<code>Request</code> objects (or :func:<code>response.follow(...)</code>) to crawl further, and dicts / :class:<code>Item</code> objects to emit data.</p>"},{"location":"reference/#crawley.spider.Spider.middlewares","title":"<code>middlewares = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Downloader middleware classes wrapping every download.</p>"},{"location":"reference/#crawley.spider.Spider.pipelines","title":"<code>pipelines = []</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Item pipeline classes applied, in order, to every emitted item.</p>"},{"location":"reference/#crawley.spider.Spider.on_item","title":"<code>on_item(item)</code>","text":"<p>Called for every item that survives the pipelines.</p>"},{"location":"reference/#crawley.spider.Spider.parse","title":"<code>parse(response)</code>","text":"<p>Default callback. Override to extract data and follow links.</p>"},{"location":"reference/#crawley.spider.Spider.start_requests","title":"<code>start_requests()</code>","text":"<p>Yield the initial requests (defaults to <code>start_urls</code>).</p>"},{"location":"reference/#crawley.spider.Request","title":"<code>crawley.spider.Request</code>","text":"<p>A scheduled HTTP request with a callback to process its response.</p>"},{"location":"reference/#crawley.spider.Request.fingerprint","title":"<code>fingerprint()</code>","text":"<p>A stable fingerprint (method + url + body) used for de-duplication.</p>"},{"location":"reference/#crawley.spider.Request.replace","title":"<code>replace(**kwargs)</code>","text":"<p>Return a copy of this request with some attributes replaced.</p>"},{"location":"reference/#crawley.spider.FormRequest","title":"<code>crawley.spider.FormRequest</code>","text":"<p>               Bases: <code>Request</code></p> <p>A :class:<code>Request</code> that submits form data (POST by default).</p>"},{"location":"reference/#crawley.spider.FormRequest.from_response","title":"<code>from_response(response, formdata=None, formid=None, formname=None, formxpath=None, callback=None, **kwargs)</code>  <code>classmethod</code>","text":"<p>Build a request from a <code>&lt;form&gt;</code> in response, pre-filling inputs.</p>"},{"location":"reference/#crawley.spider.Item","title":"<code>crawley.spider.Item</code>","text":"<p>               Bases: <code>dict</code></p> <p>A scraped item. Just a <code>dict</code> you may subclass for clarity.</p>"},{"location":"reference/#crawley.spiders.CrawlSpider","title":"<code>crawley.spiders.CrawlSpider</code>","text":"<p>               Bases: <code>Spider</code></p> <p>A spider that follows links according to a list of :class:<code>Rule</code>.</p>"},{"location":"reference/#crawley.spiders.SitemapSpider","title":"<code>crawley.spiders.SitemapSpider</code>","text":"<p>               Bases: <code>Spider</code></p> <p>Seed the crawl from <code>sitemap.xml</code> files (incl. sitemap indexes).</p>"},{"location":"reference/#crawley.spiders.LinkExtractor","title":"<code>crawley.spiders.LinkExtractor</code>","text":"<p>Extract links from a response, filtered by allow/deny rules.</p>"},{"location":"reference/#crawley.spiders.LinkExtractor.extract_links","title":"<code>extract_links(response)</code>","text":"<p>Return the (absolute, filtered) links found in response.</p>"},{"location":"reference/#crawley.spiders.Rule","title":"<code>crawley.spiders.Rule</code>","text":"<p>Bind a :class:<code>LinkExtractor</code> to a callback and/or a follow behaviour.</p>"},{"location":"reference/#pipelines-middlewares","title":"Pipelines &amp; middlewares","text":""},{"location":"reference/#crawley.pipelines.ItemPipeline","title":"<code>crawley.pipelines.ItemPipeline</code>","text":"<p>Base class for item pipelines (all methods are optional).</p>"},{"location":"reference/#crawley.pipelines.ItemPipeline.close_spider","title":"<code>close_spider(spider)</code>","text":"<p>Called once when the spider finishes.</p>"},{"location":"reference/#crawley.pipelines.ItemPipeline.open_spider","title":"<code>open_spider(spider)</code>","text":"<p>Called once when the spider starts.</p>"},{"location":"reference/#crawley.pipelines.ItemPipeline.process_item","title":"<code>process_item(item, spider)</code>","text":"<p>Return the (possibly transformed) item, or raise :class:<code>DropItem</code>.</p>"},{"location":"reference/#crawley.pipelines.DropItem","title":"<code>crawley.pipelines.DropItem</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raise from <code>process_item</code> to discard the current item.</p>"},{"location":"reference/#crawley.middlewares.DownloaderMiddleware","title":"<code>crawley.middlewares.DownloaderMiddleware</code>","text":"<p>Base class for downloader middlewares (all methods optional).</p>"},{"location":"reference/#stats-cache-throttling","title":"Stats, cache &amp; throttling","text":""},{"location":"reference/#crawley.stats.StatsCollector","title":"<code>crawley.stats.StatsCollector</code>","text":"<p>Collect counters and values during a crawl.</p>"},{"location":"reference/#crawley.stats.StatsCollector.close","title":"<code>close()</code>","text":"<p>Record the total elapsed time.</p>"},{"location":"reference/#crawley.stats.StatsCollector.open","title":"<code>open()</code>","text":"<p>Reset the stats and start the clock.</p>"},{"location":"reference/#crawley.http.cache.HttpCache","title":"<code>crawley.http.cache.HttpCache</code>","text":"<p>A tiny JSON-on-disk response cache.</p>"},{"location":"reference/#crawley.http.autothrottle.AutoThrottle","title":"<code>crawley.http.autothrottle.AutoThrottle</code>","text":"<p>Compute a per-host delay from observed latencies.</p>"},{"location":"reference/#crawley.http.autothrottle.AutoThrottle.adjust","title":"<code>adjust(host, latency)</code>","text":"<p>Update and return the new delay for host given a latency.</p>"},{"location":"reference/#extractors","title":"Extractors","text":""},{"location":"reference/#crawley.extractors.XPathExtractor","title":"<code>crawley.extractors.XPathExtractor</code>","text":"<p>               Bases: <code>BaseExtractor</code></p> <p>Extractor exposing an :mod:<code>lxml</code> tree, ready to be queried via XPath.</p>"},{"location":"reference/#crawley.extractors.CSSExtractor","title":"<code>crawley.extractors.CSSExtractor</code>","text":"<p>               Bases: <code>BaseExtractor</code></p> <p>Extractor exposing an :mod:<code>lxml</code> tree queryable with CSS selectors.</p> <p>The returned tree supports <code>tree.cssselect(\"div.foo a\")</code> thanks to the <code>cssselect</code> package.</p>"},{"location":"reference/#crawley.extractors.PyQueryExtractor","title":"<code>crawley.extractors.PyQueryExtractor</code>","text":"<p>               Bases: <code>BaseExtractor</code></p> <p>Extractor using PyQuery (a jQuery-like library for Python).</p>"},{"location":"reference/#crawley.extractors.RawExtractor","title":"<code>crawley.extractors.RawExtractor</code>","text":"<p>               Bases: <code>BaseExtractor</code></p> <p>Returns the raw html data untouched.</p>"},{"location":"reference/#http","title":"HTTP","text":""},{"location":"reference/#crawley.http.response.Response","title":"<code>crawley.http.response.Response</code>","text":"<p>Encapsulates an HTTP response.</p> <p>Attributes:</p> Name Type Description <code>raw_html</code> <p>the decoded body of the response (<code>str</code>).</p> <code>html</code> <p>the body parsed by the crawler's extractor (lxml tree, PyQuery object, ...). <code>None</code> when no extractor was used.</p> <code>url</code> <p>the final url of the request (after redirects).</p> <code>status_code</code> <p>the HTTP status code.</p> <code>headers</code> <p>the response headers.</p>"},{"location":"reference/#crawley.http.response.Response.doc","title":"<code>doc</code>  <code>property</code>","text":"<p>Return the body as a high level :class:<code>~crawley.scraping.Document</code>.</p> <p>Lets you scrape a crawler response with the modern, ergonomic API::</p> <pre><code>def scrape(self, response):\n    title = response.doc.css_first(\"h1\").text\n</code></pre>"},{"location":"reference/#crawley.http.response.Response.meta","title":"<code>meta</code>  <code>property</code>","text":"<p>The <code>meta</code> dict carried by the originating request (if any).</p>"},{"location":"reference/#crawley.http.response.Response.css","title":"<code>css(selector)</code>","text":"<p>Shortcut for <code>response.doc.css(selector)</code>.</p>"},{"location":"reference/#crawley.http.response.Response.css_first","title":"<code>css_first(selector)</code>","text":"<p>Shortcut for <code>response.doc.css_first(selector)</code>.</p>"},{"location":"reference/#crawley.http.response.Response.extract","title":"<code>extract(rules)</code>","text":"<p>Shortcut for <code>response.doc.extract(rules)</code>.</p>"},{"location":"reference/#crawley.http.response.Response.follow","title":"<code>follow(url, callback=None, **kwargs)</code>","text":"<p>Build a :class:<code>~crawley.spider.Request</code> to a (possibly relative) url.</p> <p>Relative urls are resolved against this response's url, and <code>meta</code> is inherited from the current request unless overridden.</p>"},{"location":"reference/#crawley.http.retry.RetryPolicy","title":"<code>crawley.http.retry.RetryPolicy</code>","text":"<p>Configurable retry/backoff strategy.</p>"},{"location":"reference/#crawley.http.retry.RetryPolicy.backoff_time","title":"<code>backoff_time(attempt, response=None)</code>","text":"<p>Seconds to wait before retry number attempt (0-based).</p>"},{"location":"reference/#crawley.http.retry.RetryPolicy.should_retry","title":"<code>should_retry(attempt, response=None, exception=None)</code>","text":"<p>Return <code>True</code> if a further attempt should be made.</p>"},{"location":"reference/#crawley.http.throttle.HostRateLimiter","title":"<code>crawley.http.throttle.HostRateLimiter</code>","text":"<p>Throttle requests on a per-host basis.</p>"},{"location":"reference/#crawley.http.throttle.HostRateLimiter.semaphore","title":"<code>semaphore(host)</code>","text":"<p>Return the per-host semaphore (or <code>None</code> when uncapped).</p>"},{"location":"reference/#crawley.http.throttle.HostRateLimiter.set_delay","title":"<code>set_delay(host, delay)</code>","text":"<p>Override the minimum delay for a single host (e.g. from robots).</p>"},{"location":"reference/#crawley.http.throttle.HostRateLimiter.throttle","title":"<code>throttle(host)</code>  <code>async</code>","text":"<p>Wait so that requests to host respect the configured delay.</p>"},{"location":"reference/#crawley.http.robots.RobotsPolicy","title":"<code>crawley.http.robots.RobotsPolicy</code>","text":"<p>Cache and evaluate <code>robots.txt</code> rules per host.</p>"},{"location":"reference/#crawley.http.robots.RobotsPolicy.allowed","title":"<code>allowed(url, client)</code>  <code>async</code>","text":"<p>Return <code>True</code> if url may be fetched according to robots.txt.</p>"},{"location":"reference/#crawley.http.robots.RobotsPolicy.crawl_delay","title":"<code>crawl_delay(url)</code>","text":"<p>Return the <code>Crawl-delay</code> for url's host, if any.</p>"},{"location":"scraping/","title":"Scraping API","text":"<p>The <code>crawley.scraping</code> module is the high-level, ergonomic entry point for \"just scrape this page\" use cases. It is built on the same <code>httpx</code> + <code>lxml</code> stack as the crawler but gives you a friendly, <code>parsel</code> / <code>requests-html</code> flavoured interface.</p>"},{"location":"scraping/#fetching-a-page","title":"Fetching a page","text":"<pre><code>from crawley.scraping import fetch\n\ndoc = fetch(\"https://quotes.toscrape.com/\")   # returns a Document\n</code></pre> <p><code>fetch()</code> is synchronous. For async code use <code>afetch()</code>, and to fetch many pages concurrently use <code>afetch_all()</code>:</p> <pre><code>import asyncio\nfrom crawley.scraping import afetch, afetch_all\n\ndoc = asyncio.run(afetch(\"https://example.com\"))\n\nurls = [\"https://example.com/1\", \"https://example.com/2\"]\ndocs = asyncio.run(afetch_all(urls))   # list of Document (None on error)\n</code></pre> <p>You can also parse html you already have:</p> <pre><code>from crawley.scraping import parse\n\ndoc = parse(\"&lt;html&gt;...&lt;/html&gt;\", url=\"https://example.com/\")\n</code></pre>"},{"location":"scraping/#selecting-elements","title":"Selecting elements","text":"<p>A <code>Document</code> (and any <code>Element</code>) supports CSS selectors and XPath:</p> <pre><code>doc.css(\"div.quote\")          # -&gt; list[Element]\ndoc.css_first(\"h1\")           # -&gt; Element | None\ndoc.xpath(\"//h1/text()\")      # -&gt; list (strings or Element)\ndoc.title                     # -&gt; the &lt;title&gt; text\n</code></pre> <p>Queries can be nested:</p> <pre><code>for quote in doc.css(\"div.quote\"):\n    text = quote.css_first(\"span.text\").text\n    author = quote.css_first(\"small.author\").text\n    tags = quote.css(\"a.tag::text\")\n</code></pre>"},{"location":"scraping/#pseudo-selectors","title":"Pseudo-selectors","text":"<p>Append <code>::text</code> or <code>::attr(name)</code> to a CSS selector to pull values instead of elements (just like scrapy / parsel):</p> <pre><code>doc.css(\"span.text::text\")    # -&gt; [\"The quote\", ...]\ndoc.css(\"a::attr(href)\")      # -&gt; [\"https://...\", ...]  (absolute)\n</code></pre>"},{"location":"scraping/#element-helpers","title":"Element helpers","text":"<pre><code>el = doc.css_first(\"a\")\nel.text                       # normalized recursive text\nel.attr(\"href\")               # attribute (or a default)\nel.attrs                      # dict of all attributes\nel.html                       # serialized back to html\n</code></pre>"},{"location":"scraping/#links","title":"Links","text":"<p><code>links()</code> returns the de-duplicated, absolute hrefs on the page:</p> <pre><code>doc.links()                   # all &lt;a href&gt;\ndoc.links(\"li.next a\")        # only matching a chosen selector\n</code></pre> <p>Absolute urls</p> <p>When a <code>url</code> is provided (<code>fetch</code> does this automatically), relative links are resolved to absolute urls, so <code>href=\"page2\"</code> becomes <code>https://site/dir/page2</code>.</p>"},{"location":"scraping/#declarative-extraction","title":"Declarative extraction","text":"<p><code>extract()</code> maps field names to selectors. A string selector yields a single value (the first match); a one-element list selector yields the list of every match:</p> <pre><code>doc.extract({\n    \"title\":   \"h1::text\",\n    \"price\":   \"span.price::text\",\n    \"images\":  [\"img::attr(src)\"],\n    \"authors\": [\"small.author::text\"],\n})\n# {\"title\": \"...\", \"price\": \"...\", \"images\": [...], \"authors\": [...]}\n</code></pre> <p><code>scrape()</code> fetches and extracts in a single call:</p> <pre><code>from crawley.scraping import scrape\n\ndata = scrape(\"https://example.com\", {\"title\": \"h1::text\"})\n</code></pre>"},{"location":"scraping/#inside-a-crawler","title":"Inside a crawler","text":"<p>The same shortcuts are available on the crawler's <code>response</code> object, so you can use the modern API inside a scraper's <code>scrape()</code> method:</p> <pre><code>class MyScraper(BaseScraper):\n    matching_urls = [\"%\"]\n\n    def scrape(self, response):\n        response.css_first(\"h1\").text\n        response.css(\"a::attr(href)\")\n        response.extract({\"title\": \"h1::text\"})\n        response.doc            # the underlying Document\n</code></pre>"},{"location":"spiders/","title":"Spiders (callbacks, items &amp; pipelines)","text":"<p>Besides the <code>BaseCrawler</code> + <code>BaseScraper</code> model, crawley ships a more powerful, Scrapy-style Spider built on the same async engine (httpx, retries, rate limiting, robots.txt, de-duplication).</p>"},{"location":"spiders/#requests-with-callbacks","title":"Requests with callbacks","text":"<p>A <code>Spider</code> defines <code>parse()</code> (the default callback) and yields <code>Request</code> objects \u2014 or <code>response.follow(...)</code> \u2014 to crawl further, and dicts / <code>Item</code> objects to emit data.</p> <pre><code>from crawley.spider import Spider\n\nclass BlogSpider(Spider):\n    name = \"blog\"\n    start_urls = [\"https://example.com/blog/\"]\n\n    def parse(self, response):\n        # follow each post to a detail callback\n        for href in response.css(\"a.post::attr(href)\"):\n            yield response.follow(href, callback=self.parse_post)\n        # follow pagination with the default callback (parse)\n        nxt = response.css_first(\"a.next::attr(href)\")\n        if nxt:\n            yield response.follow(nxt)\n\n    def parse_post(self, response):\n        yield {\n            \"title\": response.css_first(\"h1\").text,\n            \"url\": response.url,\n        }\n\nBlogSpider().run()          # or: await BlogSpider().start()\n</code></pre>"},{"location":"spiders/#passing-state-meta-and-cb_kwargs","title":"Passing state: <code>meta</code> and <code>cb_kwargs</code>","text":"<pre><code>def parse(self, response):\n    yield response.follow(\n        \"/item/1\",\n        callback=self.parse_item,\n        meta={\"category\": \"books\"},     # available as response.meta\n        cb_kwargs={\"rank\": 1},          # passed as a keyword argument\n    )\n\ndef parse_item(self, response, rank=None):\n    yield {\"category\": response.meta[\"category\"], \"rank\": rank}\n</code></pre> <p><code>Request</code> supports <code>method</code>, <code>data</code> (POST), <code>headers</code>, <code>priority</code>, <code>dont_filter</code> (bypass de-duplication) and <code>errback</code> (per-request error handler). De-duplication uses a fingerprint of method + url + body.</p>"},{"location":"spiders/#submitting-forms","title":"Submitting forms","text":"<p><code>FormRequest.from_response</code> reads a <code>&lt;form&gt;</code> from the page, pre-fills its inputs and lets you override fields \u2014 handy for logins and search forms:</p> <pre><code>from crawley.spider import FormRequest\n\nclass LoginSpider(Spider):\n    start_urls = [\"https://site.example/login\"]\n\n    def parse(self, response):\n        yield FormRequest.from_response(\n            response,\n            formdata={\"username\": \"me\", \"password\": \"secret\"},\n            callback=self.after_login,\n        )\n\n    def after_login(self, response):\n        ...\n</code></pre> <p><code>from_response</code> accepts <code>formid</code> / <code>formname</code> / <code>formxpath</code> to pick a specific form, and honours the form's <code>method</code> (GET forms become a query string).</p>"},{"location":"spiders/#item-pipelines","title":"Item pipelines","text":"<p>Pipelines post-process every emitted item in order. Raise <code>DropItem</code> to discard one.</p> <pre><code>from crawley.pipelines import ItemPipeline, DropItem\n\nclass PricePipeline(ItemPipeline):\n    def open_spider(self, spider): ...\n    def close_spider(self, spider): ...\n\n    def process_item(self, item, spider):\n        if not item.get(\"price\"):\n            raise DropItem(\"missing price\")\n        item[\"price\"] = float(item[\"price\"])\n        return item            # may be sync or async\n\nclass ShopSpider(Spider):\n    pipelines = [PricePipeline]\n    ...\n</code></pre> <p>Items that survive the pipeline reach <code>on_item(item)</code> (override it, or use a pipeline, to store them \u2014 e.g. with the persistence layer).</p>"},{"location":"spiders/#downloader-middlewares","title":"Downloader middlewares","text":"<p>Middlewares wrap every download: they run in order on the way out (<code>process_request</code>) and in reverse on the way back (<code>process_response</code> / <code>process_exception</code>) \u2014 like Scrapy. Use them to inject headers, rotate proxies/user-agents, short-circuit with a cached response, or recover from errors.</p> <pre><code>from crawley.middlewares import DownloaderMiddleware\nfrom crawley.spider import Request\n\nclass AuthHeaderMiddleware(DownloaderMiddleware):\n    def process_request(self, request, spider):\n        request.headers[\"Authorization\"] = \"Bearer ...\"\n        return None              # continue the download\n\n    def process_response(self, request, response, spider):\n        if response.status_code == 302:\n            return Request(response.headers[\"location\"])   # reschedule\n        return response\n\n    def process_exception(self, request, exception, spider):\n        return None              # propagate (or return a Response/Request)\n\nclass MySpider(Spider):\n    middlewares = [AuthHeaderMiddleware]\n    ...\n</code></pre> <ul> <li><code>process_request</code> -&gt; <code>None</code> (continue), a <code>Response</code> (skip the download) or a   <code>Request</code> (reschedule).</li> <li><code>process_response</code> -&gt; a <code>Response</code> or a <code>Request</code>.</li> <li><code>process_exception</code> -&gt; <code>None</code> (propagate), a <code>Response</code> or a <code>Request</code>.</li> </ul> <p>All methods may be sync or async.</p>"},{"location":"spiders/#rule-based-crawling-crawlspider","title":"Rule-based crawling: <code>CrawlSpider</code>","text":"<p><code>CrawlSpider</code> follows links automatically according to a list of <code>Rule</code>s, each built from a <code>LinkExtractor</code>.</p> <pre><code>from crawley.spiders import CrawlSpider, Rule, LinkExtractor\n\nclass SiteSpider(CrawlSpider):\n    start_urls = [\"https://books.example/\"]\n    rules = [\n        # paginate, just follow\n        Rule(LinkExtractor(allow=[r\"/catalogue/page-\\d+\"])),\n        # product pages -&gt; extract with parse_book\n        Rule(LinkExtractor(allow=[r\"/catalogue/.+/index\\.html\"]),\n             callback=\"parse_book\"),\n    ]\n\n    def parse_book(self, response):\n        yield {\"title\": response.css_first(\"h1\").text}\n</code></pre> <p><code>LinkExtractor(allow, deny, restrict_xpaths, restrict_css, tags, attrs, unique)</code> returns absolute, filtered links (<code>allow</code>/<code>deny</code> are regexes).</p>"},{"location":"spiders/#sitemaps-sitemapspider","title":"Sitemaps: <code>SitemapSpider</code>","text":"<p>Seed the crawl from one or more <code>sitemap.xml</code> files (sitemap indexes are followed automatically):</p> <pre><code>from crawley.spiders import SitemapSpider\n\nclass NewsSpider(SitemapSpider):\n    sitemap_urls = [\"https://news.example/sitemap.xml\"]\n\n    def parse(self, response):\n        yield {\"title\": response.css_first(\"h1\").text, \"url\": response.url}\n</code></pre>"},{"location":"spiders/#javascript-rendering","title":"JavaScript rendering","text":"<p>For client-side rendered / SPA sites, render pages with a headless browser (Playwright). Install the extra and the browser, then set <code>render_js = True</code>:</p> <pre><code>pip install \"crawley[js]\"\nplaywright install chromium\n</code></pre> <pre><code>class SpaSpider(Spider):\n    start_urls = [\"https://app.example/\"]\n    render_js = True\n    playwright_options = {\"browser_type\": \"chromium\", \"wait_for\": \"div.loaded\"}\n\n    def parse(self, response):\n        yield {\"title\": response.css_first(\"h1\").text}\n</code></pre> <p>Everything else (selectors, <code>follow</code>, pipelines, rules, rate limiting, retries) works the same \u2014 only the download step changes.</p>"}]}